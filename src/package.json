{
  "name": "easypick",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "client:coverage": "npm run --prefix client test -- --coverage",
    "server:coverage": "npm run --prefix server test:coverage && npm run --prefix server test:lcov",
    "combine-coverage": "npm run client:coverage && npm run server:coverage && lcov-result-merger '{client,server}/coverage/lcov.info'",
    "ci": "npm run combine-coverage | ./node_modules/coveralls/bin/coveralls.js -v"
  },
  "devDependencies": {
    "coveralls": "^2.13.1",
    "lcov-result-merger": "^1.2.0"
  }
}
